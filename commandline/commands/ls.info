list the files and folders in a directory

ls # just usual file and folders

ls *.txt # list just files with txt extension

ls -a # include the hidden ones

ls -l # more details long form

ls -tl # order them by last modified time, most recent at top

ls -rtl # reverse order them by them

ls -hl # human readable file size

ls -dl # details about current folder

ls -lhS # sort by size

ls -d */ # list just directories .*/ for hidden ones
