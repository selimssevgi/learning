# JSON Object

- Provides methods to convert string to json and  json to string


```javascript
var fidoString = '{ "name": "Fido", "breed": "Mixed" }';

var fido = JSON.parse(fidoString);

var fidoStr = JSON.stringify(fido);
```
