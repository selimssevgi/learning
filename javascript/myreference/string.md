# String


```javascript
str.length;
str.toUpperCase();
str.toLowerCase();
str.indexOf("a");                        // -1
str.slice(include, exclude);             // str
str.split(",");                          // array
```
