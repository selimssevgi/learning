- A type annotation must follow every function parameters, precede by a colon.
  Cus the compiler does not infer function parameters.

- In recursive functions, the compiler requires you to specify the result type.

def max(x: Int, y: Int): Int = {
  if (x > y)  x
  else y
}

def greet() = println("Hello World!")
