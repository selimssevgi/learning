# Activator Commands


```shell
$ activator -h | -help
$ activator ui           # bring up GUI on localhost:8888
$ activator new          # interactive project creation

$ activator new my-app-name play-java
$ activator new my-app-name play-scala

$ activator run          # run the project on localhost:9000
```
