# package.json


```shell
npm init # to create a package.json file

npm init --yes # with default values
npm init -y

npm install # install specified depends in package.json
npm install express --save # add it as a dependency
```
