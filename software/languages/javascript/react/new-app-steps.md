# New React App

- npm init

- npm install -S react react-dom # --save

- npm install webpack webpack-dev-server --save-dev

- npm install babel-core babel-loader babel-preset-env --save-dev
  - includes preset-latest, preset-react, preset-stage-0
